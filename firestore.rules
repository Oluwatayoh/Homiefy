
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access to all documents and subcollections for development/testing.
    // This allows any read or write operation regardless of authentication or data state.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
